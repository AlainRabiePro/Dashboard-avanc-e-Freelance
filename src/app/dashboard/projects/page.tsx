
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ProjectsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Projects</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Project management will be available here. You will be able to track all your projects, view their status, progress, and deadlines.</p>
      </CardContent>
    </Card>
  );
}
